# P9 UI 修复批次实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 7 项 UI 问题，涵盖会话树页面、聊天页面和设置页面。

**Architecture:** 每个任务独立修改 1-3 个文件。共享文件的 Task 需顺序执行（T1→T2, T5→T6），其余可并行。每个 task 完成后编译验证 + commit。

**Tech Stack:** Kotlin, Jetpack Compose, Material 3 (BOM 2026.05.01, M3 1.4.0)

**设计规格:** `docs/superpowers/specs/2026-06-03-p9-ui-fixes-design.md`

**编译命令:** `.\gradlew :app:compileDevDebugKotlin`
**测试命令:** `.\gradlew :app:testDevDebugUnitTest --rerun`

**编译失败处理:** 如果某 Task 编译失败：(1) `git checkout -- <本 task 涉及的所有文件>`；(2) 重新读取文件；(3) 重试。若重试仍失败，跳过该 Task 并记录。

**需求映射:** #1→T1, #2→T2, #3→T3, #4→T4, #5→T5, #9→T6, #11→T7

---

## 文件结构

| 操作 | 文件 | 任务 |
|------|------|------|
| 修改 | `ui/screens/sessions/components/OpenProjectDialog.kt` | T1, T2 |
| 修改 | `ui/screens/sessions/SessionListScreen.kt` | T2 |
| 修改 | `ui/screens/chat/ChatViewModel.kt` | T3 |
| 修改 | `ui/screens/chat/components/ChatTopBar.kt` | T3 |
| 修改 | `ui/screens/chat/ChatScreen.kt` | T3 |
| 修改 | `ui/screens/chat/tools/ToolCardRegistry.kt` | T4 |
| 修改 | `ui/screens/sessions/components/DirectoryTreeNode.kt` | T5, T6 |
| 修改 | `ui/screens/sessions/components/SessionRow.kt` | T6 |
| 修改 | `app/src/main/res/values/strings.xml` | T6 |
| 创建 | `ui/theme/ListItemTokens.kt` | T7 |
| 修改 | `ui/screens/settings/SettingsScreen.kt` | T7 |

---

### Task 1: 目录选择器根路径改为 "/"

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/OpenProjectDialog.kt:87-143`

- [ ] **Step 1: 修改初始路径和返回逻辑**

将行 87-95 的 LaunchedEffect 中 `home` → `"/"`：

```kotlin
// 修改前 (行 90):
val startDir = initialDirectory ?: home

// 修改后:
val startDir = initialDirectory ?: "/"
```

将行 117 的条件门控改为检查根目录：

```kotlin
// 修改前 (行 117):
if (currentDir != homeDir && currentDir != null) {

// 修改后:
if (currentDir != "/" && currentDir != null) {
```

将行 122 的 fallback 从 `homeDir` → `"/"`：

```kotlin
// 修改前 (行 122):
currentDir = parent.ifEmpty { homeDir }

// 修改后:
currentDir = parent.ifEmpty { "/" }
```

- [ ] **Step 2: 编译验证**

Run: `.\gradlew :app:compileDevDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/OpenProjectDialog.kt
git commit -m "fix(sessions): directory selector starts from root '/' instead of home"
```

---

### Task 2: 面包屑高度跳动 + 返回箭头始终显示

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/OpenProjectDialog.kt:110-143`
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/SessionListScreen.kt:108-154`

- [ ] **Step 1: 修复 OpenProjectDialog 路径栏**

将行 110-143 的路径栏 Row 中，把条件显隐的 `IconButton(32.dp)` 替换为始终显示的小图标：

```kotlin
// 替换行 110-143 的整个 Row 内容:
// Path bar
Row(
    modifier = Modifier
        .fillMaxWidth()
        .padding(bottom = 8.dp),
    verticalAlignment = Alignment.CenterVertically,
) {
    // Back arrow - always visible, disabled at root
    val isAtRoot = currentDir == "/"
    Icon(
        imageVector = Icons.AutoMirrored.Filled.ArrowBack,
        contentDescription = stringResource(R.string.back),
        modifier = Modifier
            .size(16.dp)
            .then(
                if (!isAtRoot) Modifier.clickable {
                    val path = currentDir?.trimEnd('/') ?: ""
                    val parent = path.substringBeforeLast('/')
                    currentDir = parent.ifEmpty { "/" }
                } else Modifier
            ),
        tint = if (isAtRoot) {
            MaterialTheme.colorScheme.onSurfaceVariant.copy(alpha = AlphaTokens.MUTED)
        } else {
            MaterialTheme.colorScheme.onSurfaceVariant
        },
    )
    Spacer(modifier = Modifier.width(4.dp))
    Text(
        text = tildeReplace(currentDir ?: "/"),
        modifier = Modifier
            .weight(1f)
            .horizontalScroll(rememberScrollState()),
        softWrap = false,
        style = MaterialTheme.typography.bodySmall,
        color = MaterialTheme.colorScheme.onSurfaceVariant,
    )
}
```

需要在文件 imports 中确认已有 `Modifier.clickable`（已有 `foundation.clickable`）。

- [ ] **Step 2: 修复 SessionListScreen TopAppBar 高度跳动**

将行 108-154 的 TopAppBar title 改用原生 subtitle 参数：

添加 import:
```kotlin
import androidx.compose.material3.ExperimentalMaterial3ExpressiveApi
```

将函数上的 `@OptIn(ExperimentalMaterial3Api::class)` 扩展为：
```kotlin
@OptIn(ExperimentalMaterial3Api::class, ExperimentalMaterial3ExpressiveApi::class)
```

替换行 109-154 的 TopAppBar：

```kotlin
TopAppBar(
    title = {
        Text(
            text = uiState.serverName.ifEmpty { stringResource(R.string.sessions_title) },
            maxLines = 1,
        )
    },
    subtitle = {
        if (uiState.baseDirectory != null) {
            Row(
                verticalAlignment = Alignment.CenterVertically,
                modifier = Modifier.clickable { showBaseDirDialog = true },
            ) {
                Text(
                    text = uiState.baseDirectory!!.replace('\\', '/').trimEnd('/'),
                    style = MaterialTheme.typography.labelSmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant,
                )
                Spacer(modifier = Modifier.width(2.dp))
                Icon(
                    imageVector = Icons.Default.ArrowDropDown,
                    contentDescription = null,
                    modifier = Modifier.size(16.dp),
                    tint = MaterialTheme.colorScheme.onSurfaceVariant,
                )
            }
        }
    },
    navigationIcon = {
        IconButton(onClick = onNavigateBack) {
            Icon(Icons.AutoMirrored.Filled.ArrowBack, contentDescription = stringResource(R.string.back))
        }
    },
)
```

**注意**: 如果 `ExperimentalMaterial3ExpressiveApi` 在 M3 1.4.0 中不存在或 `subtitle` 参数不可用，则退回原方案：保持 Column 布局但用 `AnimatedVisibility` 包裹 baseDirectory 行避免高度跳变。

- [ ] **Step 3: 编译验证**

Run: `.\gradlew :app:compileDevDebugKotlin`
Expected: BUILD SUCCESSFUL

如果 `ExperimentalMaterial3ExpressiveApi` 编译失败，使用退回方案：

需要添加 import: `import androidx.compose.animation.AnimatedVisibility`

```kotlin
// 退回方案: 保持 Column，但用 AnimatedVisibility
TopAppBar(
    title = {
        Column(modifier = Modifier.clickable { showBaseDirDialog = true }) {
            Text(
                text = uiState.serverName.ifEmpty { stringResource(R.string.sessions_title) },
                style = MaterialTheme.typography.titleMedium,
            )
            AnimatedVisibility(visible = uiState.baseDirectory != null) {
                Row(verticalAlignment = Alignment.CenterVertically) {
                    Text(
                        text = (uiState.baseDirectory ?: "").replace('\\', '/').trimEnd('/'),
                        style = MaterialTheme.typography.labelSmall,
                        color = MaterialTheme.colorScheme.onSurfaceVariant,
                    )
                    Spacer(modifier = Modifier.width(2.dp))
                    Icon(
                        imageVector = Icons.Default.ArrowDropDown,
                        contentDescription = null,
                        modifier = Modifier.size(16.dp),
                        tint = MaterialTheme.colorScheme.onSurfaceVariant,
                    )
                }
            }
        }
    },
    navigationIcon = { ... },
)
```

- [ ] **Step 4: Commit**

```bash
git add app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/OpenProjectDialog.kt app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/SessionListScreen.kt
git commit -m "fix(sessions): stable breadcrumb height, always-visible back arrow"
```

---

### Task 3: Token 进度条数据修复

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/ChatViewModel.kt:412-422, 500-504`
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/components/ChatTopBar.kt:67-71 (函数签名), 193-209 (引用)`
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/ChatScreen.kt:537-541 (传参)`

**背景**: ChatViewModel 行 403-404 注释明确说明 `session.tokens is NOT cumulative in the OpenCode backend (overwrite, not +=)`。因此正确做法是使用行 407-411 已计算的累计值（`totalXxx`），而非 `session.tokens`。当前 bug 是 `lastContextTokens` 只取了最后一条消息的 token，而非累计值。

- [ ] **Step 1: 修改 ChatViewModel.kt — lastContextTokens 计算**

替换行 412-422：

```kotlin
// 修改前 (12行):
// Context usage: total tokens from the last assistant message with output > 0
val lastWithOutput = assistantMessages.lastOrNull { (it.tokens?.output ?: 0) > 0 }
val lastTokens = lastWithOutput?.tokens
val lastContextTokens = lastTokens?.let { t ->
    t.input + t.output + t.reasoning + t.cache.read + t.cache.write
} ?: 0
val contextInputTokens = lastTokens?.input ?: 0
val contextOutputTokens = lastTokens?.output ?: 0
val contextReasoningTokens = lastTokens?.reasoning ?: 0
val contextCacheReadTokens = lastTokens?.cache?.read ?: 0
val contextCacheWriteTokens = lastTokens?.cache?.write ?: 0

// 修改后 (2行):
// Context usage: cumulative tokens from all loaded assistant messages
val lastContextTokens = totalInputTokens + totalOutputTokens + totalReasoningTokens + totalCacheReadTokens + totalCacheWriteTokens
```

- [ ] **Step 2: 修改 ChatViewModel.kt — ChatUiState 赋值**

在 ChatUiState 构造处（行 500-504 附近），删除 5 个 contextXxx 赋值行：
```kotlin
// 删除以下 5 行:
contextInputTokens = contextInputTokens,
contextOutputTokens = contextOutputTokens,
contextReasoningTokens = contextReasoningTokens,
contextCacheReadTokens = contextCacheReadTokens,
contextCacheWriteTokens = contextCacheWriteTokens,
```
这些值已被 `totalInputTokens`/`totalOutputTokens`/等（行 483-487）覆盖。

- [ ] **Step 3: 修改 ChatUiState — 删除冗余字段**

在 ChatUiState data class（行 85-89 附近），删除 5 个 contextXxx 字段定义：
```kotlin
// 删除以下 5 行（字段定义）:
val contextInputTokens: Int = 0,
val contextOutputTokens: Int = 0,
val contextReasoningTokens: Int = 0,
val contextCacheReadTokens: Int = 0,
val contextCacheWriteTokens: Int = 0,
```
这些字段与 totalXxx 字段（行 64-68）功能重复。

- [ ] **Step 4: 修改 ChatTopBar.kt — 函数参数重命名**

将 ChatTopBar 函数签名中的 5 个 contextXxx 参数改为 totalXxx：
```kotlin
// 修改前:
contextInputTokens: Int,
contextOutputTokens: Int,
contextReasoningTokens: Int,
contextCacheReadTokens: Int,
contextCacheWriteTokens: Int,

// 修改后:
totalInputTokens: Int,
totalOutputTokens: Int,
totalReasoningTokens: Int,
totalCacheReadTokens: Int,
totalCacheWriteTokens: Int,
```

同时更新 tokenRow 调用（行 193-209）中的引用名。

- [ ] **Step 5: 修改 ChatScreen.kt — 调用点参数名更新**

在 ChatScreen.kt 中 ChatTopBar 的调用处（行 537-541 附近），将参数名从 contextXxx 改为 totalXxx：
```kotlin
// 修改前:
contextInputTokens = uiState.contextInputTokens,
...

// 修改后:
totalInputTokens = uiState.totalInputTokens,
...
```

- [ ] **Step 6: 编译验证**

Run: `.\gradlew :app:compileDevDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 7: Commit**

```bash
git add app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/ChatViewModel.kt app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/components/ChatTopBar.kt app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/ChatScreen.kt
git commit -m "fix(chat): token progress uses cumulative totals from all messages"
```

---

### Task 4: 工具调用卡片标题截取文件名

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/tools/ToolCardRegistry.kt:40-69, 121-165`

- [ ] **Step 1: 修复 extractFileNameFromTitle 和 shortPath**

将行 40-47 的 `extractFileNameFromTitle` 改为兼容 Windows/Unix：

```kotlin
private fun extractFileNameFromTitle(title: String): String {
    val lastSegment = title.substringAfterLast('/').substringAfterLast('\\')
    return if (lastSegment.length < title.length && lastSegment.contains('.')) {
        lastSegment
    } else {
        title
    }
}
```

将行 69 的 `shortPath` 改为使用 `java.io.File`（注意：`java.io.File` 是 JVM 标准库，无需额外 import）：

```kotlin
// 修改前:
val shortPath = filePath?.substringAfterLast('/')

// 修改后:
val shortPath = filePath?.let { java.io.File(it).name }
```

将行 121-126 的 `list` 分支 subtitle 也做截取：

```kotlin
// 修改前:
"list", "listDirectory" -> {
    ToolDisplayInfo(
        title = serverTitle ?: stringResource(R.string.tool_list_directory),
        subtitle = filePath,
        icon = Icons.Default.Folder
    )
}

// 修改后:
"list", "listDirectory" -> {
    ToolDisplayInfo(
        title = serverTitle ?: stringResource(R.string.tool_list_directory),
        subtitle = shortPath ?: filePath,
        icon = Icons.Default.Folder
    )
}
```

- [ ] **Step 2: 编译验证**

Run: `.\gradlew :app:compileDevDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add app/src/main/kotlin/dev/minios/ocremote/ui/screens/chat/tools/ToolCardRegistry.kt
git commit -m "fix(chat): tool card titles show filename instead of full path"
```

---

### Task 5: 活动会话数 x/y 中 x 着重样式

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/DirectoryTreeNode.kt:78-98`

- [ ] **Step 1: 修改活动会话数显示**

添加 imports：

```kotlin
import androidx.compose.foundation.text.buildAnnotatedString
import androidx.compose.foundation.text.withStyle
import androidx.compose.ui.text.SpanStyle
import androidx.compose.ui.text.font.FontWeight
```

替换行 82-91（active 分支）：

```kotlin
// 修改前:
if (node.activeSessionCount > 0) {
    Text(
        text = stringResource(
            R.string.directory_session_count_active,
            node.activeSessionCount,
            node.sessionCount
        ),
        style = MaterialTheme.typography.labelSmall,
        color = MaterialTheme.colorScheme.onSurface.copy(alpha = AlphaTokens.MUTED),
    )
}

// 修改后:
if (node.activeSessionCount > 0) {
    Text(
        text = buildAnnotatedString {
            withStyle(
                SpanStyle(
                    fontWeight = FontWeight.Bold,
                    color = MaterialTheme.colorScheme.primary
                )
            ) {
                append("${node.activeSessionCount}")
            }
            withStyle(
                SpanStyle(
                    color = MaterialTheme.colorScheme.onSurface.copy(alpha = AlphaTokens.MUTED)
                )
            ) {
                append("/${node.sessionCount} sessions active")
            }
        },
        style = MaterialTheme.typography.labelSmall,
    )
}
```

注意：不再使用 `stringResource(R.string.directory_session_count_active, ...)`，改为手动拼接以支持分段样式。**已知技术债务：硬编码英文字符串 `"/${node.sessionCount} sessions active"` 绕过了 15-locale 本地化。** 使用 `buildAnnotatedString` + 分段样式后无法直接使用 `stringResource()` 的占位符功能。后续可拆分为 3 段（数字前缀 + "/" + 数字后缀）用 string resource 拼接来解决。

- [ ] **Step 2: 编译验证**

Run: `.\gradlew :app:compileDevDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 3: Commit**

```bash
git add app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/DirectoryTreeNode.kt
git commit -m "feat(sessions): highlight active session count in directory tree"
```

---

### Task 6: 长按弹窗标题优化（目录详情/会话详情）

**Files:**
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/DirectoryTreeNode.kt:120-140`
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/SessionRow.kt:191-193`

- [ ] **Step 1: 修改目录详情弹窗标题**

在 DirectoryTreeNode.kt 行 120-140，将 `title = node.displayName` 改为固定标题，原 title 内容移入 DetailRow：

```kotlin
// 修改前 (行 120-140):
AppDialog(
    onDismiss = onDismiss,
    title = node.displayName,
    ...

// 修改后:
AppDialog(
    onDismiss = onDismiss,
    title = stringResource(R.string.session_directory_details),  // "目录详情"
    ...
    content = {
        SelectionContainer {
            Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
                DetailRow(label = stringResource(R.string.session_path), value = node.path)
                DetailRow(label = stringResource(R.string.session_count), value = node.sessionCount.toString())
            }
        }
    },
```

需要新增 string resource `session_directory_details`。先检查 strings.xml 是否已有，如果没有则添加：

```xml
<string name="session_directory_details">Directory Details</string>
<string name="session_session_details">Session Details</string>
```

- [ ] **Step 2: 修改会话详情弹窗标题**

在 SessionRow.kt 行 191-193，将 `title = item.session.title ?: ...` 改为固定标题，原 title 内容移入 DetailRow：

```kotlin
// 修改前 (行 191-193):
AppDialog(
    onDismiss = onDismiss,
    title = item.session.title ?: stringResource(R.string.session_untitled),
    ...

// 修改后:
AppDialog(
    onDismiss = onDismiss,
    title = stringResource(R.string.session_session_details),
    ...
    content = {
        SelectionContainer {
            Column(verticalArrangement = Arrangement.spacedBy(4.dp)) {
                // 新增: 会话名称作为第一行
                DetailRow(
                    label = stringResource(R.string.session_details_name),
                    value = item.session.title ?: stringResource(R.string.session_untitled)
                )
                DetailRow(stringResource(R.string.session_details_id), item.session.id)
                // ... 其余行保持不变
            }
        }
    },
```

需要新增 string resource `session_details_name`：

```xml
<string name="session_details_name">Name</string>
```

- [ ] **Step 3: 添加 string resources**

在 `app/src/main/res/values/strings.xml` 中添加：

```xml
<string name="session_directory_details">Directory Details</string>
<string name="session_session_details">Session Details</string>
<string name="session_details_name">Name</string>
```

- [ ] **Step 4: 编译验证**

Run: `.\gradlew :app:compileDevDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 5: 同步翻译 (lokit)**

项目使用 15 locale 通过 `lokit.yaml` 管理翻译。新增 string resource 后需运行 `lokit` 同步：

```bash
lokit
```

如果没有 lokit CLI 或同步失败，手动将 3 个新字符串翻译到 14 个 locale 目录（`values-*/strings.xml`）。此步骤可在全部 Task 完成后统一执行。

- [ ] **Step 6: Commit**

```bash
git add app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/DirectoryTreeNode.kt app/src/main/kotlin/dev/minios/ocremote/ui/screens/sessions/components/SessionRow.kt app/src/main/res/values/strings.xml
git commit -m "feat(sessions): directory/session details dialog with fixed titles"
```

---

### Task 7: 设置页 Item 大小调为中档 + ListItemTokens

**Files:**
- Create: `app/src/main/kotlin/dev/minios/ocremote/ui/theme/ListItemTokens.kt`
- Modify: `app/src/main/kotlin/dev/minios/ocremote/ui/screens/settings/SettingsScreen.kt`

- [ ] **Step 1: 创建 ListItemTokens.kt**

```kotlin
package dev.minios.ocremote.ui.theme

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.ui.unit.dp

/**
 * Semantic padding tokens for [androidx.compose.material3.ListItem] content.
 * Three density levels matching the project's token system pattern.
 */
object ListItemTokens {
    /** Compact — minimal vertical padding. */
    val ContentPaddingSmall = PaddingValues(horizontal = 16.dp, vertical = 2.dp)
    /** Medium — balanced density (default for settings-style lists). */
    val ContentPaddingMedium = PaddingValues(horizontal = 16.dp, vertical = 4.dp)
    /** Large — Material 3 default density. */
    val ContentPaddingLarge = PaddingValues(horizontal = 16.dp, vertical = 8.dp)
}
```

- [ ] **Step 2: 在 SettingsScreen 所有 ListItem 中应用 Medium padding**

在 SettingsScreen.kt 中添加 import：

```kotlin
import dev.minios.ocremote.ui.theme.ListItemTokens
```

为每个 `ListItem(` 调用添加 `Modifier.padding()` 覆盖。**注意：标准版 `ListItem(headlineContent=...)` 没有 `contentPadding` 参数**（该参数仅存在于 Expressive 版本），必须使用 `Modifier.padding()` 替代。所有 22 处 ListItem 调用都需要修改：

```kotlin
// 在每个 ListItem 调用中添加 modifier 参数的 padding:
ListItem(
    headlineContent = { ... },
    // ... 其他参数不变
    modifier = Modifier
        .clickable { ... }
        .padding(ListItemTokens.ContentPaddingMedium),
)
```

对于已有 `modifier = Modifier.clickable { ... }` 的 ListItem，在链尾追加 `.padding(ListItemTokens.ContentPaddingMedium)`。
对于没有 modifier 的 ListItem，新增 `modifier = Modifier.padding(ListItemTokens.ContentPaddingMedium)`。

使用 grep 定位所有 ListItem 调用：`rg "ListItem\(" app/src/main/kotlin/dev/minios/ocremote/ui/screens/settings/SettingsScreen.kt`

- [ ] **Step 3: 编译验证**

Run: `.\gradlew :app:compileDevDebugKotlin`
Expected: BUILD SUCCESSFUL

- [ ] **Step 4: Commit**

```bash
git add app/src/main/kotlin/dev/minios/ocremote/ui/theme/ListItemTokens.kt app/src/main/kotlin/dev/minios/ocremote/ui/screens/settings/SettingsScreen.kt
git commit -m "feat(theme): add ListItemTokens and apply medium padding to settings"
```

---

## 执行后验证

完成所有 7 个 Task 后：

- [ ] **全量编译**: `.\gradlew :app:compileDevDebugKotlin`
- [ ] **单元测试**: `.\gradlew :app:testDevDebugUnitTest --rerun`
- [ ] **版本号无变更**（本批次不改版本号）
